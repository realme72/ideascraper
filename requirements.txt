# Dependencies are added as each stage lands, not up front.
# Current set covers stages 1 (sourcing) and 2 (enrichment).

httpx>=0.27          # HTTP client for source/enrich
pydantic>=2.7        # the stage hand-off schemas in pipeline/models.py
beautifulsoup4>=4.12 # homepage text extraction in fetchers/web.py

pytest>=8.0          # tests
